// Opening app should load the landing page, index.html, which has a link to the notes page, notes.html

// Clicking link to notes.html should open that page
// notes.html should have a left-hand column containing existing notes, along with a right-hand column containing empty fields to enter a new note title and the note's text

// Upon entering a title and text, a save icon should appear in the navbar at top of page
// Clicking save icon should save the new note (contents of the previously empty fields) and the note should appear in the left-hand column along with the other existing notes

// Clicking an existing note in the left column should open that note in the right column

// Clicking the + icon (write icon) in the navbar should bring empty fields into the right column to enter a new title and text

